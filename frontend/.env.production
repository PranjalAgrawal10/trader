# Public origin of the app (no trailing slash required). Must match how browsers reach /api/v1:
# - Same hostname as this SPA if App Platform ingress routes /api → API with preserve_path_prefix.
# - Or the API’s own URL if the API is only on a different host.
VITE_API_BASE_URL=https://traderapp-cirr5.ondigitalocean.app/
