# Trader SPA — production build template (documentation only).
# Set before `npm run build`. See frontend/.env.example.

# Same-origin (recommended): leave cross-origin vars unset.
# App Platform / nginx routes /api and /hubs to the API component.

# Split-host only (both required):
# VITE_FORCE_CROSS_ORIGIN_API=true
# VITE_API_BASE_URL=https://your-api-host.example.com

# Docker nginx runtime:
# API_UPSTREAM=https://your-api-internal-url
