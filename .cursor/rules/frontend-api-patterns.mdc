---
description: Frontend — api client, errors, list ordering, env
globs: frontend/src/**/*.{ts,tsx}
alwaysApply: false
---

# Frontend API usage

- **Client**: Use the shared axios `api` instance from `src/api/client` (base URL from `VITE_API_BASE_URL`). Do not scatter raw `fetch` to the API unless there is an existing pattern (e.g. streaming).
- **Retries**: The shared client retries **once** after a recoverable failure (**2 total attempts**): network errors (no response), **5xx**, and **408**. It does **not** retry **401** (handled by auth interceptor), **429** (the server is explicitly throttling — an immediate retry only amplifies the rate-limit pressure), other **4xx**, or **cancelled** requests. When adding calls, use `api` so this policy applies; do not add per-call retry loops that fight the global cap unless there is a strong reason (document it).
- **Polling**: Background `setInterval` polls (status, automation rows, chart live-poll, etc.) must (a) gate on `document.visibilityState === 'visible'` so they don't run in hidden tabs, and (b) be scoped to the screen actually consuming the data — e.g. only poll the auto-predictions endpoint while the **Automation** tab is mounted, not from every tab. Per-tile polls (favorites grid, etc.) should keep their cadence at **`CHART_LIVE_POLL_MS` (60s)** or slower; do not lower it without weighing the multiplied call rate.
- **Errors**: ASP.NET often returns ProblemDetails (`title`, `detail`, `status`). When handling errors, read **`detail`** (and fallbacks) like existing `problemDetail` helpers—do not assume Axios `error.response.data.message` alone.
- **Auth**: The client should already attach Bearer tokens where configured; new authenticated calls go through the same `api` instance.
- **Query params**: Match server names (`instrumentToken`, `interval`, `model`, etc.). Optional params may be omitted rather than sent empty when the server treats null as default.
- **Displayed lists**: When the API promises “newest first” or UX requires it, sort explicitly (e.g. by `predictedAt`) rather than assuming array order from merges or localStorage.
- **Extract shared helpers**: Prefer one shared `problemDetail`/`parseProblemDetails` in `src/utils/` when adding new pages rather than copying another 30-line helper.
