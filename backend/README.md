# Trader API (backend)

.NET 8 solution: **`Trader.sln`**, projects under **`src/`**, tests under **`tests/`**.

```bash
cd backend
dotnet run --project src/Trader.Api
dotnet test
```

See the repository root **`README.md`** for MySQL, Docker, JWT, and deployment.
