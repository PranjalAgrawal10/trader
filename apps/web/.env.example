VITE_API_BASE_URL=http://localhost:5232
# Production: origin where /api/v1 is reachable (same hostname as the SPA if App Platform ingress maps /api → API; or the API’s own URL).
VITE_DEV_SERVER_PORT=5173
# Optional override for Vite dev proxy only:
# VITE_API_PROXY_TARGET=http://localhost:5232
